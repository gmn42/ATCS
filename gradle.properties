org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# allow Gradle to auto-download toolchain JVMs (when resolver is available)
org.gradle.java.installations.auto-download=true
# Disable the Gradle configuration cache - this is small enough not to bother with it
org.gradle.configuration-cache=false